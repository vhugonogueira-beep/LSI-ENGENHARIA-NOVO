[637] // ── Limpa orcSel se orçamento foi deletado
[639] if (orcSel && !historico.find(o => o.id === orcSel)) {
[642] }, [orcSel, historico]);
[726] const [orcSel, setOrcSel] = React.useState(null); // orçamento aberto no detalhe
[3546] if (orcSel) {
[3547] const orc = historico.find(o => o.id === orcSel);
